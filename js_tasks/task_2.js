function task2 (word1, word2) {
    let word1_letters = [];
    for (let letter of word1) {
        if (letter in word1_letters) {
            continue
        }
        word1_letters.push(letter);
    }

    let word1_letters_number = []

    for (let letter of word2) {
        word2_letters.push(letter);
    }
}